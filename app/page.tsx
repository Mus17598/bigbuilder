/**
 * STEP 4 — hero plus the pinned service rack. Deep-dive scrub beats,
 * analytics gallery, process, proof, pricing and the CTA land in steps 5-7.
 */
import Hero from '@/components/Hero';
import ServiceRack from '@/components/ServiceRack';

export default function Page() {
  return (
    <main id="main">
      <Hero />

      <ServiceRack />
    </main>
  );
}
